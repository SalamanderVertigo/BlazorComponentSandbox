
@if (Open && ChildContent is not null)
{
    <div class="jg-dialog-container">
        @ChildContent
    </div>
}

@code {
    [Parameter] public bool Open { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }
}